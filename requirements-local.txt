# Локальная проверка без Docker и PostgreSQL (БД = SQLite).
# Сознательно БЕЗ asyncpg: он нужен только для Postgres и требует компилятора на Windows.
# Пины ослаблены (>=), чтобы pip взял готовые wheel под любую версию Python.
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.31
alembic>=1.13
pydantic>=2.9
pydantic-settings>=2.4
aiosqlite>=0.20
pytest>=8
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.15.22
black>=24
